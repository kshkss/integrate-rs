[package]
name = "radau-sys"
version = "0.1.0"
edition = "2021"
links = "radau"
build = "build.rs"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[features]
default = []
lapack = []

[dependencies]
libc = "0.2"

[build-dependencies]
cc = "1.0"
